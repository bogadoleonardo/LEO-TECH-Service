import {$,$$,setMode,setEditing} from "./core.js";import {renderDashboard,renderHistory} from "./dashboard.js";
export function showView(id){const target=document.getElementById(id);if(!target)return false;document.querySelectorAll(".view").forEach(v=>v.classList.remove("active-view"));target.classList.add("active-view");document.querySelectorAll(".nav-item").forEach(b=>b.classList.toggle("active",b.dataset.view===id));const titles={dashboard:"Inicio","new-service":"Nuevo servicio",history:"Historial técnico",clients:"Clientes",equipment:"Equipos"};$("#pageTitle").textContent=titles[id]||"Inicio";if(id==="dashboard")renderDashboard();if(id==="history")renderHistory();window.scrollTo(0,0);return false}
export function selectMode(v){setMode(v);$$(".mode").forEach(b=>b.classList.toggle("active",b.dataset.mode===v));const f=$("#serviceForm");f?.classList.remove("mode-quick","mode-normal","mode-complete");f?.classList.add("mode-"+v)}
export function openNew(){setEditing(null);showView("new-service");selectMode("quick");$("#serviceForm").reset()}
